# deep copy list
self.nums = oldNums[:]

# random number generator
import random
random.randrange(0, i+1)

# yield v/s return

# Generators v/s iterators

#Path compression
https://www.cs.princeton.edu/courses/archive/spring13/cos423/lectures/UnionFind.pdf

## L[::-1] ? reverse list w/ copy
https://docs.python.org/2.3/whatsnew/section-slices.html
- use of range to init array

## Zip operator
eg w/ 2 d array pick y column from start row
zip(*t[::1])

eg w/ 2 d array pick y column from end row
zip(*t[::-1])

## n-d array initialization

2. ndArray = [[[0 for _ in xrange(n)] for _ in xrange(n) ] for _ in xrange(n)]

1. ndArray = [[[0]*n]*n]*n ## TODo Was creating references validate

#To preety print array
import pprint
pprint.print()
